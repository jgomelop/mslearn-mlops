$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json
name: xray-custom-training-env
version: "2.0.3"
description: Custom build using Dockerfile to ensure requirements.txt is found.

build:
  path: .
  dockerfile_path: Dockerfile