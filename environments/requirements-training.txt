# MLflow for model tracking and logging (pinned to 2.7.1 for Azure ML compatibility)
mlflow==2.7.1
azureml-mlflow

# PyTorch and vision (already in base image curated environment)
# torch==2.8.0 is pre-installed (requires Python 3.10+)
# torchvision==0.21.0 is pre-installed

# X-ray specific library
torchxrayvision

# ML utilities
scikit-learn
pandas
numpy

# Image processing
Pillow

# DO NOT INSTALL azureml-defaults (causes Flask dependency conflict with MLflow 2.7.1)